{
  "name": "benefits-builders-program",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9",
  "scripts": {
    "dev:web": "pnpm --filter @bb/web dev",
    "dev:worker": "pnpm --filter @bb/worker dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "migrate": "echo \"(placeholder)\"",
    "seed": "echo \"(placeholder)\""
  }
}
