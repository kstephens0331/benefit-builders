{
  "buildCommand": "pnpm exec turbo run build --filter=@bb/web || (cd apps/web && pnpm exec next build)",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/web/.next"
}
