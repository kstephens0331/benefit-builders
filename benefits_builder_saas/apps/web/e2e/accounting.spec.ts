// E2E tests require a running application - run with 'pnpm test:e2e'
import { test } from '@playwright/test';

test.describe.skip('E2E Tests', () => {
  test('placeholder', () => {
    // Tests skipped - run separately with test:e2e command
  });
});
